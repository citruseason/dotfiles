# modules/macos/homebrew/Brewfile — 공통 패키지 (모든 프로필)
# 항목 추가/삭제 후 `dotfiles --tags homebrew` 실행

# ── CLI Tools ──
brew "git"
brew "git-lfs"
brew "diff-so-fancy"
brew "mas"
brew "wget"
brew "tree"
brew "dockutil"
brew "mise"
brew "mole"
brew "nmap"
brew "tmux"

# ── Shell ──
brew "zsh"
brew "starship"
brew "vivid"
brew "coreutils"

# ── Editor ──
brew "neovim"

# ── Container ──
# nerdctl은 별도 formula 대신 `colima nerdctl install`로 설치 (macOS bottle 미지원)
brew "kubectl"
brew "colima"

# ── Apps ──
cask "ghostty"
cask "google-chrome"
cask "slack"
cask "notion"
cask "iina"
cask "visual-studio-code"
cask "scroll-reverser"
cask "appcleaner"

# ── Fonts ──
cask "font-hack-nerd-font"
