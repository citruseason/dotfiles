---
- name: Enable systemd in WSL
  ansible.builtin.shell: |
    sudo tee /etc/wsl.conf > /dev/null << 'CONF'
    # BEGIN ANSIBLE MANAGED - WSL boot config
    [boot]
    systemd=true
    # END ANSIBLE MANAGED - WSL boot config
    CONF
  changed_when: false
