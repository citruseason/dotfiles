---
- name: Check if Homebrew is installed
  ansible.builtin.command: which brew
  register: brew_check
  changed_when: false
  failed_when: false

- name: Install Homebrew
  ansible.builtin.shell: >
    NONINTERACTIVE=1 /bin/bash -c
    "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  when: brew_check.rc != 0

- name: Add Homebrew to PATH (Apple Silicon)
  ansible.builtin.shell: eval "$(/opt/homebrew/bin/brew shellenv)"
  when:
    - brew_check.rc != 0
    - ansible_facts['architecture'] == 'arm64'
  changed_when: false

- name: Remove deprecated taps
  ansible.builtin.command: brew untap {{ item }}
  loop:
    - homebrew/cask-versions
    - homebrew/cask-fonts
  register: untap_result
  changed_when: untap_result.rc == 0
  failed_when: false

- name: Update Homebrew
  community.general.homebrew:
    update_homebrew: true

- name: Install formulae
  community.general.homebrew:
    name: "{{ homebrew_formulae }}"
    state: present

- name: Install casks
  community.general.homebrew_cask:
    name: "{{ homebrew_casks }}"
    state: present
    install_options: adopt

- name: Install private casks
  community.general.homebrew_cask:
    name: "{{ homebrew_private_casks }}"
    state: present
    install_options: adopt
  when: homebrew_install_private | bool

- name: Install Mac App Store apps
  community.general.mas:
    id: "{{ item.id }}"
    state: present
  loop: "{{ homebrew_mas_apps }}"
  loop_control:
    label: "{{ item.name }}"
  when: homebrew_install_private | bool
  ignore_errors: true
  register: mas_result

- name: Show Mac App Store manual install notice
  ansible.builtin.debug:
    msg: >-
      다음 앱은 App Store에서 직접 설치하세요:
      {{ mas_result.results | default([])
         | selectattr('failed', 'defined')
         | selectattr('failed')
         | map(attribute='item')
         | map(attribute='name')
         | list | join(', ') }}
  when:
    - homebrew_install_private | bool
    - mas_result.results | default([]) | selectattr('failed', 'defined') | selectattr('failed') | list | length > 0
